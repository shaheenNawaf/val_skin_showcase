// CardForge runtime configuration.
// The Supabase URL + anon key are public identifiers (protected by RLS) and are
// safe to commit. NEVER put the service-role key in this file.
// Until these are filled in, the app runs in localStorage-only mode:
// listings are shareable per-browser via view.html?slug=...
window.CARDFORGE_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
